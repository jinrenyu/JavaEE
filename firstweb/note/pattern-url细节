1. 一个<servlet>元素可以同时拥有多个<servlet-mappint>
映射元素, 且每个映射不冲突的情况下都可以生效, 访问一个Servlet类

2. 对已匹配模式的通配符, 具有以下两种方式:
a.) 以'/'打头, 并且以'*'结尾
b.) *.后缀(这个可以匹配任何以指定后缀结尾的请求)

3. 优先级
1.) 当用户请求同时匹配多个请求路径的时候, 匹配度高的路径具有优先权,
即路径越具体, 其优先级越高
2.) '*.后缀'匹配方式, 其优先级最低

4. load-on-startup元素标签
这个标签是属于<servlet>的子元素标签, 标签中间书写的是数字
如果这个数字是整数并且大于0, 则应用服务器会在启动之后, 立刻初始化
这个servlet实例, 数字越小越早初始化.
这种定时或者完成系统初始化任务的servlet不是用于给用户进行访问,
所以我们一般采用实现Servlet接口的方式实现, 并且不会会其配置
<servlet-mapping>元素标签.
配置如下:
<load-on-startup></load-on-startup>